$('.delete').bind('ajax:success', function(){
	$(this).closest('post').fadeOut();
});
$('.lead').html('currently there are <%= j pluralize(Post.all.count, "post")%> from <%= j pluralize(User.all.count, "user")%>');